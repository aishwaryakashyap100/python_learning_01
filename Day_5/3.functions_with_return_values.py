# Function to add two numbers and return result
def add_numbers(a, b):
    result = a + b
    return result                         # Sends back the value

# Store the returned result
total = add_numbers(5, 10)
print("Total:", total)                    # Output: 15